/**
 * spec document for ByteArray.ts class
 */


import { ByteArray } from "../../../../../src/implementation/graphics/images/byte-array";
import { PdfImage, PdfBitmap, PdfDocument, PdfGraphics, PdfPage } from '../../../../../src/index';

describe('ByteArray.ts', () => {
    describe('Constructor initializing',()=> {
        let t1 : ByteArray = new ByteArray(10);
        let t2 : ByteArray = new ByteArray(20);
    })
})